# IP Checker System - Система проверки интеллектуальной собственности

## 📋 Описание

Веб-приложение для проверки товарных знаков и изображений на предмет возможных нарушений интеллектуальной собственности. Система использует OCR для распознавания текста на изображениях, обратный поиск изображений и проверку по базам товарных знаков.

## 🚀 Возможности

### Проверка наименований
- Проверка текстовых названий по базам товарных знаков
- Поддержка классов МКТУ (Международная классификация товаров и услуг)
- Интеграция с ФИПС, Роспатент, Linkmark, WIPO
- Цветовая индикация рисков (🔴 красный, 🟡 жёлтый, 🟢 зелёный)

### Проверка изображений
- Загрузка изображений (drag & drop или выбор файла)
- OCR распознавание текста на изображениях (EasyOCR)
- Обратный поиск изображений через Google (Serper.dev API)
- Фильтрация некачественных OCR результатов (порог уверенности 60%)
- Проверка распознанного текста по базам товарных знаков

### История и отчёты
- Сохранение всех проверок в базу данных SQLite
- Просмотр истории проверок с фильтрацией по статусу
- Экспорт отчётов в Excel и PDF
- Статистика проверок

## 🛠 Технологии

- **Backend:** Python 3, Flask
- **Frontend:** HTML5, Bootstrap 5, JavaScript
- **OCR:** EasyOCR
- **База данных:** SQLite
- **Reverse Image Search:** Serper.dev API
- **Экспорт:** openpyxl (Excel), reportlab (PDF)
- **Публикация:** Cloudflare Tunnel

## 📁 Структура проекта

```
ip_checker_system/
├── src/
│   ├── app.py              # Главный файл Flask приложения
│   ├── config.py           # Конфигурация и API ключи
│   ├── database.py         # Работа с SQLite базой данных
│   ├── image_checker.py    # OCR и проверка изображений
│   └── trademark_checker.py # Проверка товарных знаков
├── templates/
│   ├── index.html          # Главная страница
│   └── history.html        # Страница истории проверок
├── uploads/                # Загруженные изображения
├── ip_checker.db           # База данных SQLite
├── cloudflared.log         # Лог туннеля Cloudflare
└── README.md               # Этот файл
```

## ⚙️ Установка

### 1. Клонирование и настройка окружения

```bash
cd /Users/olgaandreeva/Documents/Claude/Search\ trademarks\ and\ picture/ip_checker_system
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Настройка API ключей

Отредактируйте файл `src/config.py`:

```python
SERPER_API_KEY = "ваш_ключ_serper"  # Для обратного поиска изображений
```

Получить ключ: https://serper.dev/

### 3. Запуск сервера

```bash
cd src
python app.py
```

Сервер запустится на http://localhost:5001

## 🌐 Публикация в интернет

### Через Cloudflare Tunnel (рекомендуется)

1. Установка cloudflared:
```bash
brew install cloudflare/cloudflare/cloudflared
```

2. Запуск туннеля:
```bash
cloudflared tunnel --url http://localhost:5001
```

3. Скопируйте URL вида `https://xxx-xxx-xxx.trycloudflare.com`

### Запуск в фоне:
```bash
nohup cloudflared tunnel --url http://localhost:5001 > cloudflared.log 2>&1 &
```

### Получение текущего URL:
```bash
grep -o "https://.*trycloudflare.com" cloudflared.log | tail -1
```

### Остановка туннеля:
```bash
pkill cloudflared
```

## 📖 Использование

### Проверка наименования

1. Откройте главную страницу
2. Введите название для проверки
3. Выберите классы МКТУ (опционально)
4. Нажмите "Проверить"
5. Просмотрите результаты и ссылки для ручной проверки

### Проверка изображения

1. Перейдите на вкладку "Проверка изображения"
2. Загрузите изображение (drag & drop или кнопка)
3. Система автоматически:
   - Распознает текст на изображении
   - Выполнит обратный поиск
   - Проверит текст по базам ТЗ
4. Экспортируйте отчёт в Excel или PDF

### История проверок

1. Нажмите кнопку "История" в навигации
2. Переключайтесь между вкладками "Наименования" и "Изображения"
3. Фильтруйте по статусу риска
4. Просматривайте детали и экспортируйте отчёты

## 🎨 Цветовая индикация рисков

| Цвет | Статус | Значение |
|------|--------|----------|
| 🔴 Красный | HIGH | Высокий риск нарушения ИС |
| 🟡 Жёлтый | MEDIUM | Требуется дополнительная проверка |
| 🟢 Зелёный | LOW | Низкий риск |

## 🔧 Конфигурация

### Порог уверенности OCR

В файле `src/app.py` (строка ~180):
```python
if text_item.confidence < 0.60:  # Изменить порог здесь
    continue
```

### Ресурсы для проверки

Настраиваются в `src/config.py` в словаре `RESOURCES`.

## 📊 API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/` | Главная страница |
| GET | `/history` | Страница истории |
| POST | `/api/check/name` | Проверка наименования |
| POST | `/api/check/image` | Проверка изображения |
| GET | `/api/history/stats` | Статистика проверок |
| GET | `/api/history/names` | История проверок наименований |
| GET | `/api/history/images` | История проверок изображений |
| GET | `/api/export/image/<id>/<format>` | Экспорт отчёта изображения |
| GET | `/api/export/name/<id>/<format>` | Экспорт отчёта наименования |
| DELETE | `/api/history/<type>/<id>` | Удаление записи |

## 🐛 Решение проблем

### Сервер не запускается
```bash
# Проверить, не занят ли порт
lsof -i :5001
# Убить процесс если нужно
kill -9 <PID>
```

### OCR не работает
```bash
# Переустановить EasyOCR
pip uninstall easyocr
pip install easyocr
```

### Туннель не создаётся
```bash
# Проверить интернет-соединение
curl -I https://cloudflare.com
# Переустановить cloudflared
brew reinstall cloudflared
```

## 📝 Лицензия

Проект создан для личного использования.

## 👤 Автор

Создано с помощью Claude AI (Anthropic)

---

**Текущий публичный URL:** Проверьте файл `cloudflared.log` для актуальной ссылки.
